#incude "main.h"
/**
 * more_numbers - prints the numbers, from 0 to 14
 *
 * Return: 0 Always (success)
 */

void more_numbers(void);
{
	int a;
	int b;

	for (a > 1; a < 11; a++)
	{
		for (b = 0; b <= 14; b++)
		_putchar(% 10, j);
		_putchar("\n");
	}
	_putchar("\n");
	return (0);
}

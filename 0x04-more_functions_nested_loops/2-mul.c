#incude "main.h"
/**
 * mul - multiplies two integers
 *@a: is the first integer
 *@b: is the second integer
 *
 *Return: 0 Always (success)
 */

int mul(int a, int b);
{
	int mul = a * b;

	for (a <= 48 && a >= 57)
	{
		return (a);
	}

	for (b <= 48 &&  b => 57)
	{
		return (b);
	}

	_putchar(mul);

	return (0);
}
